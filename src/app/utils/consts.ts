export const IAConstApi = {
  styleId: 68,
  size: '1-1',
};

export enum ActionBtn {
  find_in_page = 'find_in_page',
  edit = 'edit',
  delete = 'delete',
}

export enum LocalStorageKey {
  userData = 'userData',
}

export enum FormDetails {
  UTC = 'UTC',
  DateFormat = 'yyyy-MM-dd',
}

export enum ImageUrlFixed {
  noImage = './no-image.jpg',
}
